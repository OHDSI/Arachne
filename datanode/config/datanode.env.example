# DataNode local environment variables (git-ignored).
# Copy to datanode.env in this directory. Source before running: source config/datanode.env
# Or use config-local.yml (copy from config-local.example.yml) to set the same values in YAML.
#
# Docker: backend uses arachne.docker.host (default Unix socket). Override only if you need TCP:
# ARACHNE_DOCKER_HOST=unix:///var/run/docker.sock
#
# Study Repository: catalog URL, username, and token (picked up by application.yml when set).
# These auto-populate the Study Repository settings in the UI; users can change and save them there.
# ARACHNE_DOCKER_REGISTRY_URL=https://registry.example.com
# ARACHNE_DOCKER_REGISTRY_USER=your-registry-username
# ARACHNE_DOCKER_REGISTRY_TOKEN=your-registry-token
